version: '3.8'

services:
  drift-chat:
    build: .
    ports:
      - "3150:3000"  # Using port 3150 for drift.abhinavaditya.com
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    container_name: drift-chat